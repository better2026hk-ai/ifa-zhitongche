# IFA直通车 小程序 —— 开发记录与项目说明

> 这份文档是给"未来的你"和"未来的 Claude"看的。以后想加功能、改题库、
> 修 bug，把这个文档（或者这个仓库）交给 Claude，说清楚你想要什么，
> 就可以直接继续开发，不用从头解释一遍。
>
> 文档分两部分：**第一部分**是给你自己看的，用大白话说清楚"这东西是
> 什么、数据存在哪、密钥在哪、以后要改题库怎么办"；**第二部分**是给
> Claude 看的技术细节（数据库结构、接口列表等），你不用全看懂，但别
> 删掉，Claude 需要这些信息才能准确接手。

---

## 第一部分：给你自己看的（大白话版）

### 这是什么项目

"IFA直通车"是一个微信小程序，给香港保险中介人资格考试的考生用的刷题
App。功能：模拟考试、章节练习、错题本、个人学习记录。

### 代码放在哪、怎么找到它

- GitHub 仓库：`better2026hk-ai/ifa-zhitongche`
- 开发分支：`claude/ifa-wechat-mini-app-c49xul`（所有开发都在这个分支，
  没有合并到 main）
- 小程序代码在仓库的 `miniprogram/` 文件夹里，这是你用微信开发者工具
  打开的那个文件夹。
- 后端代码（数据库相关）在仓库的 `supabase/` 文件夹里。

### 数据存在哪、为什么这么存

一开始想用"微信云开发"存数据（微信官方提供的免费数据库），但**你的
主体是香港（海外）主体，微信云开发不给海外主体开通**，所以换成了
第三方数据库服务 **Supabase**（一个跟微信没关系的独立数据库服务商，
国外的，不受这个限制）。

现在的数据流转是这样的：

```
用户手机上的小程序  ──发请求──>  Supabase 上的一个"云函数"  ──读写──>  Supabase 数据库
```

小程序本身不直接连数据库，所有读写都要经过 Supabase 上部署的那"一个"
云函数（专业说法叫 Edge Function，代码在 `supabase/functions/api/index.ts`），
这个函数负责："这个人是谁"（登录验证）+ "该给他看什么数据/该帮他存什么数据"。

### 密钥和账号信息在哪里——这些东西非常重要，丢了要重新配一遍

这些**不在代码里**，只存在于 Supabase 后台，你要自己记住/保管好：

| 名称 | 在哪配置 | 是什么 |
|---|---|---|
| Supabase 账号 | supabase.com | 你注册的账号，登录后能看到项目 |
| Supabase 项目 | supabase.com 后台 | project ref 是 `rzltdhwgizmqtwqbllra`（这个不是密钥，写在代码里也没事） |
| `WX_APPID` | Supabase 后台 → Edge Functions → Secrets | 小程序的 AppID |
| `WX_SECRET` | 同上 | 小程序的 AppSecret（微信公众平台后台查到的，**绝密**，只在这里填过，没告诉过 Claude） |
| `SESSION_SECRET` | 同上 | 你自己生成的一串随机字符串，用来给用户登录凭证签名，**绝密**，只有你知道 |
| Supabase 的 `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` | Supabase 自动提供，不用你管 | 云函数连数据库用的，Supabase 后台自带，不用手动配 |

**如果有一天云函数报奇怪的错、或者你要换一个 Supabase 项目**，去
Supabase 后台的 Edge Functions 页面，把这三个 Secret（`WX_APPID`、
`WX_SECRET`、`SESSION_SECRET`）重新填一遍就行，不用改代码。

代码里唯一放了"半公开信息"的文件是 `miniprogram/utils/api-config.js`：
- `API_BASE`：你的 Supabase 云函数地址
- `ANON_KEY`：Supabase 的"匿名公开密钥"（这个设计上就是可以公开的，
  不是敏感信息，跟 `WX_SECRET`/`SESSION_SECRET`那种真密钥不是一回事）

### 以后要更新题库，应该怎么做

**题库数据现在不在小程序代码里，存在 Supabase 数据库的 `questions`
这张表里。** 更新题库＝改数据库里的数据，不是改小程序代码，不需要
重新上传小程序、也不用重新审核。分两种情况：

**情况一：只是改一两道题**（改个错别字、调整答案/解析、删一道题）
直接去 Supabase 后台 →「Table Editor」→ `questions` 表，跟 Excel 一样
找到那一行改掉/删掉，最快，不用找 Claude。

**情况二：批量换一整份题库/一次加几百题** —— 把新题库文件（格式跟
`handoff/question-banks/` 里原来那 5 份一样：一个数组，每题有
`chapter`/`chapter_title`/`stem`/`options`（`{A,B,C,D}`）/`answer`/
`explanation`）发给 Claude，跟它说清楚：
- 这批题属于哪个科目（`paperKey`：`p1`/`p2`/`p3`/`p5`/`mpf`，五个
  之一，如果是全新的第 6 门科目，告诉 Claude 你想用什么 key）
- 这是"新增"还是"整份替换掉这门科目原来的题"

Claude 应该做的事（这样以后不管哪个 Claude 会话接手，照着做就行）：
1. 用仓库里现成的脚本 `supabase/seed/generate-insert-sql.js` 生成 SQL：
   ```
   node supabase/seed/generate-insert-sql.js <paperKey> <题库json路径> [--replace]
   ```
   如果是"整份替换掉这门科目原来的题"，一定要加 `--replace`（脚本会
   在 INSERT 前面自动加一句删除这门科目旧数据的 SQL，不然新旧题目会
   堆在一起、章节练习的题号顺序也会乱）；如果是全新科目直接新增，
   不用加这个参数。
2. 把生成的 SQL 发给用户（或者存成文件发过去）。
3. 用户打开 Supabase 后台 → SQL Editor → 把 SQL 整段粘进去 → 运行。
4. 提醒用户：如果这批题库涉及新的 `paperKey`（比如加了第 6 门科目），
   小程序代码里 `miniprogram/utils/banks.js` 的 `HOME_BANKS`/`EXAM_CONFIG`
   也要加一条对应配置（科目名称、总题数、模拟考试时长/合格线等），
   这部分需要改小程序代码、重新上传，不是单纯改数据库就够。如果只是
   给已有的 5 门科目换题/加题，`banks.js` 里的 `total` 字段（每门科目
   总题数）也要跟着改成新的实际题数，否则首页/模拟考试列表显示的
   "已练习 X/Y 题"会不准。

### 以后要改功能/修 bug，应该怎么做

1. 直接跟 Claude 说你想要什么（比如"我想加一个xxx功能"、"这个页面
   显示不对，截图给你"）。
2. Claude 改完代码会自动 commit + push 到 `claude/ifa-wechat-mini-app-c49xul`
   这个分支。
3. 你在微信开发者工具里 `git pull` 一下这个分支，清一下缓存，重新
   预览/编译，测试没问题就可以上传提交审核。
4. **如果这次改动涉及 Supabase 那边**（新建表、改云函数），Claude 会
   明确告诉你需要去 Supabase 后台做哪几步操作（这几步 Claude 没法
   替你做，因为需要你的账号权限）。

### 目前还没做的事情（以后可能要做）

- **绑定手机号**：原始设计文档里提过，但一直没做，现在也没有真的用
  手机号登录/绑定的功能，只用微信授权登录。
- 意见反馈现在只是存进数据库，没有做"自动通知你"的功能（比如收到反馈
  自动发消息提醒你）——如果需要可以后续加一个云函数做这个。

---

## 第二部分：给 Claude 看的技术细节

### 技术栈

- 小程序：**原生 WXML/WXSS/JS**，明确没有用 Taro 之类的框架。
- `app.json` 里 `tabBar.custom: true`，用 `custom-tab-bar/` 自定义
  底部 tab（不是微信原生 tabBar）。
- `lazyCodeLoading: "requiredComponents"` 已开启（微信代码质量检测
  建议项；项目里没有任何页面用 `usingComponents` 声明自定义组件，唯一
  的自定义组件 `custom-tab-bar` 走的是 `tabBar.custom` 专属加载机制，
  不受这个开关影响，所以是零风险的改动）。
- 题库题目页面（`packageExam/pages/flow` 模拟考试、`packageExam/pages/practice`
  章节练习）放在 `subPackages` 分包里。
- 后端：Supabase（Postgres 数据库 + 1 个 Deno Edge Function），**不是**
  微信云开发（海外主体开不通，踩过这个坑）。
- 字体：`fonts/font-data.js` 里是 Plus Jakarta Sans + JetBrains Mono
  两款字体的 base64，已经用 `pyftsubset` 按实际用到的字符集裁剪过
  （可打印 ASCII + Ⅰ Ⅱ Ⅲ Ⅴ · — › ✔ ✕ 这几个特殊符号），从 ~1.4MB
  裁到 ~440KB——这两款字体不含中文字形，中文一直是系统字体兜底显示。

### 认证机制（这是跟一般 Supabase 项目不一样的地方，容易踩坑）

微信小程序里没有邮箱/密码，只有 `wx.login()` 换来的一个临时 `code`。
Supabase 官方的 Auth 体系不认识微信身份，所以这里是**手写的一套简化
认证**，不是 Supabase Auth：

1. 小程序调 `wx.login()` 拿到 `code`（`utils/util.js` 的 `wxLoginAsync()`）。
2. 把 `code` 发给 Edge Function 的 `login` 这个 action。
3. Edge Function 用 `WX_APPID`+`WX_SECRET` 调微信官方接口
   `https://api.weixin.qq.com/sns/jscode2session` 换到这个用户的
   `openid`（微信给每个用户在每个小程序下分配的唯一 ID，不会变）。
4. Edge Function 自己签一个 token（HMAC-SHA256，密钥是 `SESSION_SECRET`，
   payload 是 `{openid, exp}`，半年过期），返回给小程序。
5. 小程序把这个 token 存本地（`wx.storage`），以后每次请求都带着它。
6. Edge Function 收到请求先验这个 token（`verifyToken()`），验出
   `openid`，再用这个 `openid` 去数据库里查/改**只属于这个人**的数据。

**关键点**：4 张跟用户相关的表（`users`/`wrongBook`/`examHistory`/
`feedback`）都开了 RLS（行级安全）但**不加任何策略**——意思是默认
拒绝所有人访问，只有 Edge Function 用的 `service_role` 密钥能穿透
（这个密钥只存在于 Edge Function 的运行环境里，从来不下发给小程序
客户端）。所以哪怕有人拿到你小程序里那个 `ANON_KEY`（本来就是公开的），
也读不到任何用户数据，必须经过 Edge Function 的 token 校验这一关。

Edge Function 部署时关掉了 Supabase 平台自带的 JWT 校验
（`supabase/config.toml` 里 `verify_jwt = false`，但**这个配置只有
用 CLI 部署才生效，网页版"Via Editor"部署不会读这个文件**）。因为
一开始踩过这个坑（网页部署导致所有请求都被 Supabase 网关拦截报
`UNAUTHORIZED_NO_AUTH_HEADER`），所以补了一个更稳的办法：小程序端
每次请求固定带上 Supabase 的 `anon` key 作为 `Authorization: Bearer`
头（`utils/store.js` 的 `callApi()` 函数），这样不管用哪种方式部署
都能过 Supabase 网关这一层，跟自己的 token 校验是两层独立的东西。

### 数据库结构（5 张表，都在 Supabase 的 Postgres 里）

字段名统一用**驼峰命名**（跟小程序 JS 对象字段名一一对应，没有做
下划线/驼峰转换层，减少出错空间）。建表 SQL 在 `supabase/schema.sql`
（4 张用户相关表）、`supabase/schema_questions.sql`（题库表）、
`supabase/schema_practice_progress.sql`（给 `users` 表加的一列，都是
后来单独追加的迁移文件，不在最初的 `schema.sql` 里）。

**`users`** —— 一个用户一行
- `openid` (主键)、`nickname`、`avatarUrl`
- `firstLoginAt`（首次完善资料的时间，毫秒时间戳）
- `paperStats`（jsonb，形如 `{p1:{done,correct}, p2:{...}, ...}`，
  每个科目做了多少题/对了多少题）
- `activityDates`（string[]，做过题的日期列表，用来算连续打卡天数）
- `accountId`（"账号管理"页展示用的一个友好 ID，首次建档时随机生成）
- `practiceProgress`（jsonb，形如 `{p1:{chapterIdx,qIndex}, ...}`，
  章节练习模式"上次做到哪"，用来做断点续练）

**`wrongBook`** —— 错题本，一条错题一行
- `id`（主键的一部分，是 `paperKey + '_' + 题干哈希值`，同一题重复
  答错只更新这一行，不会重复插入）
- 主键实际是**复合主键 `(openid, id)`**——注意不是单独 `id` 主键，
  因为不同用户答错同一道题会算出同样的哈希，必须靠 `openid` 一起
  区分开，不然会撞主键。
- `paperKey`/`paperTag`/`paperName`/`stem`/`options`/`answer`/
  `lastPicked`/`explanation`/`wrongAt`

**`examHistory`** —— 模拟考试记录，一次交卷一行
- `paperKey`/`paperTag`/`paperName`/`config`/`questions`/`answers`/
  `total`/`correct`/`passed`/`timestamp`
- `questions`/`answers` 是这次考试完整的题目和作答内容（jsonb），
  所以"逐题复盘"功能不用回头查题库，历史记录本身就是完整快照。

**`feedback`** —— 意见反馈，一条反馈一行
- `rating`/`text`/`submittedAt`

**`questions`** —— 题库，一道题一行（这是后来加的，最早题库是打包
在小程序代码里的，因为分包超过 2MB 限制才搬过来）
- `paperKey`（p1/p2/p3/p5/mpf）
- `idx`（在这门科目里的原始顺序，章节练习模式靠这个保证题号顺序对）
- `chapter`/`chapterTitle`/`stem`/`options`/`answer`/`explanation`

这 5 张表都开了 RLS 但不加策略（只有 Edge Function 能读写，见上面
"认证机制"一节）。

### Edge Function 的接口（`supabase/functions/api/index.ts`）

小程序只请求这**一个** HTTP 地址（`utils/api-config.js` 里的
`API_BASE`），body 里的 `action` 字段决定具体做什么：

| action | 做什么 | 需要登录 token |
|---|---|---|
| `login` | code 换 openid，签发 token | 不需要（这是唯一的例外） |
| `getUser` | 查当前用户的 users 行 | 需要 |
| `saveProfile` | 建档/更新昵称头像 | 需要 |
| `fetchPracticeQuestions` | 章节练习——整份卷子按 idx 排序返回 | 需要 |
| `fetchExamQuestions` | 模拟考试——服务端随机抽 N 题返回（不是整份题库都传给小程序） | 需要 |
| `answerQuestion` | 单题作答——一次请求同时处理"统计 +1"和"错题本增删" | 需要 |
| `saveProgress` | 保存章节练习"上次做到第几章第几题"，用于断点续练 | 需要 |
| `fetchWrongBook` | 拉全量错题本 | 需要 |
| `removeWrongBookEntry` | 删一条错题 | 需要 |
| `countWrongBook` | 只要错题总数（不拉全部数据） | 需要 |
| `fetchExamHistory` | 拉全量考试记录 | 需要 |
| `recordExamResult` | 交卷——聚合统计更新 + 批量错题本增删 + 写一条考试记录，固定几次数据库调用，不管卷子多少题 | 需要 |
| `submitFeedback` | 提交意见反馈 | 需要 |

对应到小程序端 `utils/store.js` 里的同名/近似名函数，页面代码只调
`store.xxx()`，不直接拼 HTTP 请求。

### 小程序端数据层：`utils/store.js`

这是唯一一个跟后端打交道的文件，页面代码统一 `require` 这个文件。
分三类函数：

1. **本机同步函数**（`isLoggedIn`/`setLoggedIn`/`logout`）——只是"这
   台设备是否同意过协议并点过登录"的本机开关，跟 Supabase 里的用户
   数据是两件事，不跨设备。
2. **云端异步函数**（`fetchUserDoc`/`saveProfile`/`answerQuestion`/
   `recordExamResult`/`fetchWrongBook`/... 等）——都是 `async`，内部
   调 `callApi()` 发请求，页面里要 `await`。`fetchUserDoc()` 有个
   模块内缓存（`_cachedUser`），同一次小程序运行期间不会重复请求。
3. **纯计算函数**（`getBankStats`/`computeOverviewStats`/`daysSinceFirstLogin`/
   `computeStreak`/`getWrongBookForPaper`/`getWrongBookGrouped` 等）——
   同步、不摸网络，接收已经拉回来的数据做计算，页面拉一次数据后可以
   反复调用这些函数重新渲染，不用每次都发请求。

### 开发过程中踩过的坑（按时间顺序，避免以后重蹈覆辙）

1. **`wx.loadFontFace` 包内路径加载失败** → 改用 base64 data URI 内嵌
   字体，后来发现体积太大又用 `pyftsubset` 裁剪字符集。
2. **WXML 属性值不能有字面换行符** → 真机编译报语法错误，检查所有
   `class="..."` 之类的属性值不能跨行写。
3. **Android 上 `env(safe-area-inset-top)` 不可靠** → 改用
   `wx.getWindowInfo().statusBarHeight` 精确测量，每个页面自己算好
   传进 `padding-top`。
4. **`open-type="chooseAvatar"` 按钮不听 CSS 宽高，渲染成椭圆** → 外面
   套一层 `overflow:hidden` + `border-radius:50%` 的容器裁出圆形。
5. **微信云开发在香港/海外主体下开不通**（`webapi_getwxaasyncsecinfo`
   报错、"海外主体无法开通云服务"）→ 整个数据层从"本地缓存"→"微信
   云开发"→ 最终换成"Supabase"，来回改了两次数据层实现，但页面代码
   基本没大改（`store.js` 对外接口一直保持稳定）。
6. **Supabase Edge Function 网页版部署不读 `config.toml`** → 网关层
   JWT 校验报 `UNAUTHORIZED_NO_AUTH_HEADER` → 固定带上 anon key 解决，
   不依赖找后台的开关。
7. **题库数据打包进小程序分包，超过 2MB 上传限制** → 题库搬进 Supabase
   `questions` 表，按需请求，不再打包进代码。
8. **主包大小超过 1.5MB 质量检测** → 字体裁剪 + 图片压缩 + 开启
   `lazyCodeLoading`。
9. **审核驳回：登录页默认勾选同意协议、首页强制登录** → 改成默认不
   勾选的勾选框 + "先浏览、用到功能再登录"的整体交互调整（首页/模拟
   考试列表允许游客浏览，错题本/我的页游客态显示登录提示而不是强制
   跳转）。
10. **意见反馈页星星图标显示成几个巨大重叠的图标** → `.star-btn` 只
    设了容器尺寸没设里面 `image` 的尺寸，微信小程序 `image` 组件没
    设宽高时默认 300×225px，补上 `width/height:100%` 解决。
11. **模拟考试横幅图里画死了"4门可用/3,092道真题"，跟实际题库
    （5门/3,748题）不一致** → 裁掉图片里画死数字的部分，换成从
    `HOME_BANKS` 现算的真实徽标。
12. **章节练习答错题后点"下一题"没反应，答对却正常** → 根因是这个
    按钮用的是原生 `<button disabled="{{!answered}}">`，而模拟考试
    的翻页按钮从来不用原生 `disabled`（用普通 `view` + CSS class 模拟
    禁用外观，JS 里自己判断范围），原生 `disabled` 在部分设备/基础库
    版本下有过状态刷新不可靠的先例。把练习模式的翻页也换成跟模拟考试
    一样的写法，并顺带放开了"必须先作答才能翻页"这个限制（跟模拟考试
    的自由导航保持一致）。这次改动也顺手加上了题号导航面板（正确/
    错误/未答三色）和断点续练（`users.practiceProgress` 字段 +
    `saveProgress` action）。
13. **错题本列表页科目名称显示成"null"** → `store.js` 的 `answerQuestion`
    （章节练习/错题本测验答题共用的函数）一直没把 `paperTag`/`paperName`
    传给服务端，导致这两种场景写进 `wrongBook` 的这两个字段一直是
    `null`（模拟考试交卷走的是另一个函数 `recordExamResult`，一直有
    传，没这个问题）。补上以后新答错的题不会再是 null，但已经写进数据库
    的旧记录需要跑一次 `supabase/fix_wrongbook_null_names.sql` 补数据。
14. **题号导航面板题目一多（超过~60题）就拉不动，剩下的题号看不到/点不到**
    → `styles/exam.wxss` 里的 `.qgrid`（题号导航的网格容器，模拟考试和
    章节练习共用同一份样式）只有外层 `.sheet` 设了 `max-height:78vh`，
    `.qgrid` 自己没设 `overflow-y:auto`，内容超出高度既不能滚动也看不到。
    补上 `max-height:56vh; overflow-y:auto;`（跟旁边 `.sheet-list` 的
    写法保持一致）就好了，一次改动两个页面都修好。
15. **自定义导航栏（`navigationStyle:"custom"`）右上角控件被微信自带的
    胶囊按钮（⋯ 和圆点）挡住/盖住点不到**——练习模式的"第X章"胶囊、
    模拟考试答题页的"交卷"按钮、错题本"选择错题组卷"页的"全选"按钮、
    首页头像，这几个页面顶部右侧原来只留了 `.exam-top`/`.review-top`/
    `.home-scroll` 那个通用的 40rpx 右内边距，远远不够躲开胶囊按钮
    （胶囊按钮不占页面布局，是悬浮在最上层的，自定义导航栏必须自己
    用 `wx.getMenuButtonBoundingClientRect()` 量出它的实际位置才能准确
    避开，不能靠猜一个固定数字）。`utils/util.js` 加了 `getMenuButtonGap()`
    算出"从屏幕右边缘算要留多少 px"，这几个页面进入时算一次存进
    `menuGap`，给顶部那一行的右侧内边距叠上这个值。以后新加"顶部右侧
    有个按钮/头像"这种页面，要记得也这么处理一下，不能默认小程序会
    自动帮你避开胶囊按钮。
16. **Paper Ⅰ 每道题都出现两次** → `questions` 表里 p1 的数据被重复导入
    了一遍（最可能是 `supabase/seed/p1.sql` 在 SQL Editor 里被跑了两次，
    表本来没有唯一性约束挡这种重复插入）。用
    `supabase/check_and_fix_duplicate_questions.sql` 先查每个科目实际
    行数确认范围，再按 `(paperKey, idx)` 去重（只留 id 最小的一条），
    最后给这两个字段加了唯一约束，以后同一份导入脚本不小心跑两遍会
    直接报错，不会再静默产生重复数据。**以后跑 `supabase/seed/` 或
    `generate-insert-sql.js` 生成的导入 SQL 之前，先用这个脚本第一步
    的 `select ... group by "paperKey"` 查一下这门科目是不是已经导入
    过了，避免重复跑。**

### 功能补充：章节练习支持"再点一次取消选择"

用户反馈"选了某一章之后没法取消选择、回到全部题目"。第一版做的是在
"切换章节"面板列表末尾加一个虚拟的"全部章节"选项——用户明确不想要
这种效果，要求改成：点选某一章后面出现对号，再点一次这个已经选中的
章节，对号取消，回到"全部题目"，不要在列表里多一行可选项。

最终实现（`packageExam/pages/practice/practice.js`）：`chapterIdx` 除了
0、1、2...这些真实章节下标，多了一个哨兵值 `-1`，代表"全部章节"（没
锁在某一章里，`items` 是整份题库）。这个伪章节存成实例属性
`this._allChapter`（`onLoad` 里跟 `chapters` 一起算出来，不进 `data`，
也不进 `chapters` 数组本身），所有按下标取章节的地方都改成走
`this.chapterAt(idx)` 这个方法（`idx === -1` 时返回 `this._allChapter`，
否则返回 `this.data.chapters[idx]`），而不是直接 `chapters[chapterIdx]`。
`switchChapter` 里判断"点的这一章是不是当前已选中的那一章"，是的话
新下标就是 `-1`，否则正常切换到点的那一章。

`.wxml` 的切换面板不用改：列表只渲染真实章节（`wx:for="{{chapters}}"`），
勾选态判断是 `index === chapterIdx`——`chapterIdx` 变成 `-1` 时天然没有
任何一行能匹配上，对号自动消失，顶部章节徽标退回显示"全部章节"，不用
额外加逻辑。断点续练存的 `practiceProgress.chapterIdx` 也可能是 `-1`，
`saveProgress`/Edge Function 那边只是存一个数字，不用改。

### 原始设计资料在哪

`handoff/` 文件夹里是最初的交接资料，包括：
- `技术交接文档.md`（原始需求文档，很多功能细节都是照这个文档做的，
  比如"答对自动移除错题要在 3 个地方实现"这种规则）
- `IFA直通车_小程序整合原型.html`（HTML 原型，界面还原参考）
- `question-banks/`（最早的题库 json 原始文件，现在题库真实数据已经
  搬进 Supabase 了，这几个文件留作备份/参考）
- `用户协议与隐私政策/`（协议文本来源）
- `assets/`（设计稿原始图片素材）
